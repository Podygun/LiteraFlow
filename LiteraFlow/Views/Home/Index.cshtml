@model bool
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>
        @if (Model)
        {
            <h1>Авторизован</h1>
        }
        else
        {
            <h1>Не Авторизован</h1>

        }
    </p>
</div>
